# Add to the user-owned moonraker.conf AFTER [include moonraker/base.conf].
# Stage only: create/validate this promotion branch before repointing the printer.
# Keep the currently verified path, managed_services and install_script settings.
[update_manager Klippain]
origin: https://github.com/agreen/klippain.git
primary_branch: codex/3dp-04-stable
